# GB28181 订阅管理架构 - PHP 层方案

## 架构对比

### ❌ 旧方案：C 层管理（已废弃）

```c
// exosip_wrapper.h
#define MAX_SUBSCRIPTIONS 1024
SubscriptionInfo subscriptions[MAX_SUBSCRIPTIONS];  // 静态数组
```

**问题：**
- 硬编码上限 1024 个订阅
- 内存浪费（预分配 1024 个槽位）
- O(n) 查找效率低
- 进程重启丢失数据

### ✅ 新方案：PHP 层管理（推荐）

```php
// Redis 存储，无上限
$subscriptionManager = new SubscriptionManager($sipServer, $redis);
```

**优势：**
- ✅ 无订阅数量限制（支持上万甚至百万级）
- ✅ Redis 持久化（进程重启不丢失）
- ✅ 分布式共享（多节点访问同一订阅状态）
- ✅ O(1) 哈希查找效率
- ✅ 灵活扩展（自定义参数、过期策略）

---

## 职责分离

| 层级 | 职责 | 理由 |
|------|------|------|
| **C 层** | 纯 SIP 协议操作 | eXosip 内部状态（dialog_id）只能在 C 层访问 |
| **PHP 层** | 业务状态管理 | Redis/MySQL 更适合存储、查询、持久化 |

### C 层保留的功能

```c
// exosip_wrapper.c
int sip_send_subscribe();           // 发送 SUBSCRIBE 请求
int sip_refresh_subscribe();        // 刷新订阅（需要 dialog_id）
int sip_cancel_subscribe();         // 取消订阅（需要 dialog_id）
int sip_send_notify_response();     // 响应 NOTIFY 请求
```

### PHP 层新增功能

```php
// SubscriptionManager.php
public function subscribe();            // 调用 C 层 + 存储到 Redis
public function refresh();              // 调用 C 层 + 更新 Redis
public function unsubscribe();          // 调用 C 层 + 删除 Redis
public function autoRefreshAll();       // 定时器自动刷新
public function cleanupExpired();       // 清理过期订阅
public function getStatistics();        // 统计和监控
```

---

## 数据存储结构

### Redis Key 设计

```
# 订阅信息（Hash）
gb28181:subscription:{deviceId}:{eventType}
  → {subscription_id, dialog_id, expires, created_at, ...}

# 刷新队列（ZSet，按时间排序）
gb28181:subscription:refresh_queue
  → {score=next_refresh_time, member=subscription_key}
```

**示例：**
```redis
# 设备 34020000001320000001 的 Catalog 订阅
HSET gb28181:subscription:34020000001320000001:Catalog \
  subscription_id 12345 \
  dialog_id 67890 \
  device_id 34020000001320000001 \
  event_type Catalog \
  expires 3600 \
  created_at 1736400000 \
  last_refresh 1736400000 \
  next_refresh 1736403400

# 加入刷新队列（按 next_refresh 时间排序）
ZADD gb28181:subscription:refresh_queue 1736403400 \
  "gb28181:subscription:34020000001320000001:Catalog"
```

---

## 使用示例

### 1. 基本订阅操作

```php
use Gb28181\GateWay\Device\Subscribe\SubscriptionManager;

$redis = new Redis();
$redis->connect('127.0.0.1', 6379);

$subscriptionManager = new SubscriptionManager($sipServer, $redis, [
    'auto_refresh' => true,
    'refresh_advance' => 300,  // 提前 5 分钟刷新
]);

// 订阅设备目录
$result = $subscriptionManager->subscribe($device, 'Catalog', 3600);
// 返回：['subscription_id' => 12345, 'device_id' => '...', ...]

// 订阅报警事件（带参数）
$result = $subscriptionManager->subscribe($device, 'Alarm', 3600, [
    'start_priority' => 1,
    'end_priority' => 3,
    'alarm_method' => '1',
]);

// 订阅移动位置（每 10 秒上报）
$result = $subscriptionManager->subscribe($device, 'MobilePosition', 3600, [
    'interval' => 10,
]);
```

### 2. 查询订阅

```php
// 查询单个订阅
$subscription = $subscriptionManager->getSubscription($deviceId, 'Catalog');
// 返回：['subscription_id' => 12345, 'dialog_id' => 67890, ...]

// 查询设备的所有订阅
$allSubs = $subscriptionManager->getSubscription($deviceId);
// 返回：[['event_type' => 'Catalog', ...], ['event_type' => 'Alarm', ...]]

// 检查订阅是否存在
if ($subscriptionManager->exists($deviceId, 'Catalog')) {
    echo "订阅存在";
}
```

### 3. 自动刷新（定时器）

```php
$sipServer->onTimer = function() use ($subscriptionManager) {
    // 自动刷新所有需要续订的订阅
    $result = $subscriptionManager->autoRefreshAll();
    // 返回：['refreshed' => 5, 'failed' => 0, 'total' => 5]
    
    // 定期清理过期订阅
    $cleaned = $subscriptionManager->cleanupExpired();
    // 返回：['cleaned' => 2]
};
```

### 4. 更新 dialog_id（200 OK 响应）

```php
$sipServer->onResponse = function($event) use ($subscriptionManager) {
    if ($event->getCode() == 200) {
        $session = $event->getSession();
        $subscriptionId = $session->getCallId();
        $dialogId = $session->getDialogId();
        
        // 更新 Redis 中的 dialog_id（用于后续 refresh/cancel）
        $subscriptionManager->updateDialogId($subscriptionId, $dialogId);
    }
};
```

### 5. 处理 NOTIFY 事件

```php
$sipServer->onNotify = function($event) use ($sipServer) {
    $body = $event->getBody();
    $xml = simplexml_load_string($body);
    
    $cmdType = (string)$xml->CmdType;
    
    switch ($cmdType) {
        case 'Catalog':
            // 设备目录变更通知
            echo "目录变更: SumNum=" . (string)$xml->SumNum;
            break;
            
        case 'Alarm':
            // 报警事件通知
            echo "报警: Priority=" . (string)$xml->AlarmPriority;
            break;
            
        case 'MobilePosition':
            // 移动位置上报
            echo "位置: {$xml->Longitude}, {$xml->Latitude}";
            break;
    }
    
    // 响应 200 OK
    $sipServer->sendNotifyResponse($event->getTid(), 200);
};
```

### 6. 取消订阅

```php
// 取消单个订阅
$subscriptionManager->unsubscribe($device, 'Catalog');

// 取消设备的所有订阅
$result = $subscriptionManager->unsubscribeAll($device);
// 返回：['canceled' => 3, 'failed' => 0, 'total' => 3]
```

### 7. 统计和监控

```php
$stats = $subscriptionManager->getStatistics();
// 返回：
// [
//   'total' => 1234,
//   'by_type' => ['Catalog' => 500, 'Alarm' => 400, 'MobilePosition' => 334],
//   'refresh_queue_size' => 1234
// ]
```

---

## 集成到 GB28181Handler

```php
// gb28181_subscription_integration.php

// 1. 设备注册后自动订阅
$sipServer->onRegister = function($event) use ($subscriptionManager, $deviceManager) {
    $deviceId = extractDeviceId($event->getFromUri());
    $device = $deviceManager->getDevice($deviceId);
    
    // 自动订阅设备目录
    $subscriptionManager->subscribe($device, 'Catalog', 3600);
};

// 2. 定时器自动刷新
$sipServer->onTimer = function() use ($subscriptionManager) {
    $subscriptionManager->autoRefreshAll();
    $subscriptionManager->cleanupExpired();  // 定期清理
};

// 3. 处理 NOTIFY 通知
$sipServer->onNotify = function($event) use ($sipServer) {
    $body = $event->getBody();
    // 解析 XML，处理业务逻辑...
    $sipServer->sendNotifyResponse($event->getTid(), 200);
};
```

---

## 性能分析

### 扩展性对比

| 方案 | 订阅上限 | 查询复杂度 | 持久化 | 分布式 |
|------|---------|-----------|--------|--------|
| C 层数组 | 1024 | O(n) | ❌ | ❌ |
| Redis Hash | 无限制 | O(1) | ✅ | ✅ |

### 内存占用对比

```
# C 层（1024 个槽位预分配）
1024 * sizeof(SubscriptionInfo) ≈ 1024 * 256 bytes = 256 KB（固定）

# PHP 层（Redis 动态分配）
10 个订阅 ≈ 10 * 500 bytes = 5 KB
10,000 个订阅 ≈ 10,000 * 500 bytes = 5 MB
```

### 支持设备规模

| 订阅类型 | 每设备订阅数 | 支持设备数 | 总订阅数 |
|---------|-------------|-----------|---------|
| 仅 Catalog | 1 | 10,000 | 10,000 |
| Catalog + Alarm | 2 | 10,000 | 20,000 |
| 全订阅 (3 类) | 3 | 10,000 | 30,000 |

**Redis 完全支持！C 层只能支持 1024 个。**

---

## 自动刷新机制

### 刷新时机

```
expires=3600, refresh_advance=300
创建时间: 2026-01-09 10:00:00
过期时间: 2026-01-09 11:00:00
刷新时间: 2026-01-09 10:55:00  （提前 5 分钟）
```

### 刷新队列（ZSet）

```redis
ZADD refresh_queue 1736403400 "subscription:device1:Catalog"
ZADD refresh_queue 1736403500 "subscription:device2:Alarm"
ZADD refresh_queue 1736403600 "subscription:device3:MobilePosition"

# 定时器查询需要刷新的订阅（分数 <= 当前时间）
ZRANGEBYSCORE refresh_queue 0 1736403400
```

---

## 测试文件

1. **基础测试**：`test_subscription_manager.php`
   - 订阅、查询、刷新、取消
   - 统计和监控

2. **集成测试**：`gb28181_subscription_integration.php`
   - GB28181Handler 完整集成
   - 定时器自动刷新
   - NOTIFY 事件处理
   - Task 异步任务

---

## 运行测试

```bash
# 1. 启动 Redis
redis-server

# 2. 运行基础测试
php examples/gbvr-iot/test_subscription_manager.php

# 3. 运行集成测试
php examples/gbvr-iot/gb28181_subscription_integration.php
```

---

## 迁移指南

如果你之前使用 C 层订阅功能，迁移步骤：

1. **安装 Redis**（如果未安装）
2. **引入 SubscriptionManager**
3. **替换调用**：
   ```php
   // 旧代码（C 层）
   $sipServer->subscribe($toUri, $eventType, $expires);
   
   // 新代码（PHP 层）
   $subscriptionManager->subscribe($device, $eventType, $expires);
   ```
4. **添加定时器回调**：
   ```php
   $sipServer->onTimer = function() use ($subscriptionManager) {
       $subscriptionManager->autoRefreshAll();
   };
   ```
5. **更新 dialog_id**：
   ```php
   $sipServer->onResponse = function($event) use ($subscriptionManager) {
       // 收到 200 OK 后更新 dialog_id
       $subscriptionManager->updateDialogId($subscriptionId, $dialogId);
   };
   ```

---

## FAQ

### Q1: C 层订阅功能还能用吗？
**A:** 可以！C 层 API 保持不变：
- `subscribe()`, `refreshSubscribe()`, `cancelSubscribe()` 仍然可用
- 但不再有 `getSubscriptions()` 等查询功能
- 推荐使用 PHP 层管理，C 层只做协议操作

### Q2: Redis 挂了怎么办？
**A:** 订阅信息丢失，但 SIP 会话仍在 eXosip 中：
- 设备侧订阅仍有效（设备会继续发送 NOTIFY）
- Redis 恢复后，重新订阅即可
- 生产环境建议使用 Redis 集群 + 持久化

### Q3: 多节点部署如何共享订阅状态？
**A:** 使用 Redis 集中存储：
```
Node 1 (Worker) ──┐
                  ├──> Redis (共享订阅状态)
Node 2 (Worker) ──┘
```

### Q4: 自动刷新失败怎么办？
**A:** 失败会记录日志：
```php
$result = $subscriptionManager->autoRefreshAll();
// ['refreshed' => 5, 'failed' => 2, 'total' => 7]
```
- 失败原因可能：设备离线、网络问题
- 下次定时器会重试（订阅仍在刷新队列）

---

## 总结

✅ **PHP 层管理订阅 = 无限扩展 + 持久化 + 分布式**  
✅ **C 层专注 SIP 协议 = 职责清晰 + 代码简洁**  
✅ **完全向后兼容 = C 层 API 不变 + PHP 层增强**

推荐所有新项目使用 PHP 层订阅管理！
